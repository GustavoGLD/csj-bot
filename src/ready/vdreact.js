const logger  = require("wax-logger");
const handler = require('wax-command-handler');

//const {vdr_events_model} = require("../database/models/vdreact");

let vdr_events;
module.exports = async () => {
    /*
    console.log(vdr_events_model);
    vdr_events = new vdr_events_model({
        count: 0,
        chatID: "856658304348454913"
    });

    reaction.save(function (err, events) {
        if (err) return console.log(`addvideoreaction: mongoose: ${err}`);
    });
    */
};

exports.vdr_events = vdr_events;